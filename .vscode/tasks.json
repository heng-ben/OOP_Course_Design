{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "编译OOP项目",
            "type": "shell",
            "command": "g++",
            "args": [
                "-g",
                "-std=c++17",
                "-fexec-charset=GBK",
                "-I", "${workspaceFolder}/include",
                "${workspaceFolder}/src/*.cpp",
                "-o", "${workspaceFolder}/build/main.exe"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": ["$gcc"],
            "detail": "编译src下所有cpp文件，include目录作为头文件搜索路径"
        }
    ]
}
